<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.rainsc.spzx.manager.Mapper.ProductMapper">
    <sql id="findPageWhere">
        <where>
            <if test="brandId != null and brandId != ''">
                and p.brand_id = #{brandId}
            </if>
            <if test="category1Id != null and category1Id != ''">
                and p.category1_id = #{category1Id}
            </if>
            <if test="category2Id != null and category2Id != ''">
                and p.category2_id = #{category2Id}
            </if>
            <if test="category3Id != null and category3Id != ''">
                and p.category3_id = #{category3Id}
            </if>
            and p.is_deleted = 0
        </where>
    </sql>
    <!--    条件分页查询-->
    <select id="findByPage" resultType="org.rainsc.spzx.model.entity.product.Product">
        select p.id,
               p.name,
               p.brand_id,
               p.category1_id,
               p.category2_id,
               p.category3_id,
               p.unit_name,
               p.slider_urls,
               p.spec_value,
               p.status,
               p.audit_status,
               p.audit_message,
               p.create_time,
               p.update_time,
               p.is_deleted,
               b.name  brandName,
               c1.name category1Name,
               c2.name category2Name,
               c3.name category3Name
        from product p
                 left join brand b on p.brand_id = b.id
                 left join category c1 on c1.id = p.category1_id
                 left join category c2 on c2.id = p.category2_id
                 left join category c3 on c3.id = p.category3_id
        <include refid="findPageWhere">
        </include>
        order by id desc
    </select>
</mapper>